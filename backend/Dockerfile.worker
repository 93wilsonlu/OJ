FROM python:3.12-slim

RUN apt-get update && apt-get install -y g++ gcc && rm -rf /var/lib/apt/lists/*

WORKDIR /app

RUN pip install uv

COPY pyproject.toml .
RUN uv sync --no-dev

COPY lib/ lib/
COPY worker.py .

CMD ["sh", "-c", "uv run python worker.py"]
